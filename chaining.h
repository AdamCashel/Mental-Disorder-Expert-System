#include <iostream>
#include <string>
#include <vector>
#include "variable.h"

extern void instantiate(std::vector<Variable> &, std::vector<int> &);

extern int determine_member_concl_list(std::string, std::vector<std::string>);